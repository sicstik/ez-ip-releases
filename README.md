## About EZ-IP

An IT Utility Suite for managing network adapter settings without memorizing terminal syntax or dealing with multiple applications for common networking tasks. 

Switch between saved address profiles, toggle DHCP, assign static IPs, manage routes, run bandwidth tests, connect to remote devices (SSH, Telnet, Serial, SFTP, FTP) and more — all from one compact, elevated window.

**Download** the latest installer from the [Releases](../../releases) page.

The app requests administrator elevation at launch. This is required to read and write network configuration.

---

## Free Features

- **Adapter management** — list all adapters with live status, enable/disable via right-click

- **IP configuration** — see current IP settings and easily toggle between DHCP / Static with a confirmation diff before applying

- **Route management** — view, add, and remove IPv4 routes. Manage multiple Gateways properly with ease

- **Profiles** — save any IP config as a named profile and apply it in one click to any adapter

- **Tray icon** — apply profiles and check adapter IPs without opening the app through the taskbar icon menu

- **Remote Sessions** — Connect to devices via SSH, Telnet, Serial, SFTP, and FTP

- **Terminal** — run terminal commands directly in-app

- **Simple Ping** - Easily test connectivity to devices without ever leaving the app

- **Auto-update** — notified when a new version is available and installs with one click

## Pro Features (One Time License Key Purchase)
- **Network Test** — Test internet speed, see public IP, test open TCP/UDP ports, and local iperf3 LAN speed tests
- **Scanner** - A full ping scan of subnets feature and continuous multi IP ping feature with verbose up down timestamped logs 

- **Discovery** - Discover devices and network info such as VLAN ID, System Name, Neighbors etc.

- **Troubleshoot** - Use personal free AI API to help troubleshoot network issues

- **Cloud sync** — Sync profiles and routes, SSH Sessions and more with other machines running EZ-IP

- **Multi tenant License Key** - Use one license key for multiple installs (Up to 3)

---
